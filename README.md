Sources for the plugin `plugin.appodeal.TwitterMoPub`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.TwitterMoPub" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
